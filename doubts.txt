TO DOS:

NORMAL
> wall collision improve
> Press two keys at the same time
> Textures that are smaller than SCALE square
> Textures that are larger than SCALE square
> textures that are rectangles
> function that converts rgb to int

BONUS
> separate bonus from normal
> Makefile bonus
> Doors which can open and close
> animated sprite
> Rotate the point of view with the mouse